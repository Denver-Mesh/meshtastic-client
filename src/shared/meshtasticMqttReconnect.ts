/** Default reconnect budget for both MQTT protocols. */
export const MQTT_DEFAULT_RECONNECT_ATTEMPTS = 3;
/** Maximum reconnect attempts allowed for both MQTT protocols. */
export const MQTT_MAX_RECONNECT_ATTEMPTS = 5;

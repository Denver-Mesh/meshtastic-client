# Cursor indexing exclusions (see AGENTS.md section 9).
# Aligns with .claudeignore where possible. Logs under .cursor/ stay openable via explicit path or @.

# From .gitignore + dependency/build/lock/log exclusions
# (node_modules etc. are ignored by default; reference explicitly when needed.)

# Local Cursor plugin/debug logs (large; keep out of default index)

*.db
.DS_Store
.aider*
.claude/
CLAUDE.md
Thumbs.db
dist/
dist-electron/
node_modules/
release/
test-results/

# Dependency and build artifacts
coverage/
.next/
